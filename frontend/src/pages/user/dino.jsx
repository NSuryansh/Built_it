import React, { memo } from "react";
import ChromeDino from "react-chrome-dino";

const Dinogame = memo(() => {
  return (
    <div>
      <ChromeDino />
    </div>
  );
});

export default Dinogame;
